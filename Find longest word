function findLongestWord(str) {
  stringArray = str.split(' ');
  orderedArray = stringArray.sort((a, b) => {
    return a.length < b.length;
  });
  return orderedArray[0].length; 
}
findLongestWord("The quick brown fox jumped over the lazy dog");
